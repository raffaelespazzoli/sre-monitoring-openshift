apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: grafana-dashboard-cluster-total
  namespace: {{ .Release.Namespace }}
  labels:
    app: grafana
spec:
  name: cluster-total.json
  json: {{ .Files.Get "dashboards/cluster-total.json" | toPrettyJson }}
